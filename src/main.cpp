#include <iostream>
#include "VectorTest.hpp"

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);

    VectorTest();
    
    std::cout << "Hello, Sílvio!" << std::endl;

    return 0;
}